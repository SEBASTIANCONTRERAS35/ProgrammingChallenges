#include <iostream>
#include <cfloat>
#include <vector>
using namespace std;
/*
int solution(const vector<int>& a){
    double distance=DBL_MAX;
    int output;
for (int i=0;i<a.size();i++){
    int x=a[i];
    int diference=0;
    for (int j=0;j<a.size();j++){
        diference+=abs(a[j]-x);
    };
    if (diference<distance){
        distance=diference;
        output=x;
    }
}
return output;
}

int main() {
    // Casos de prueba predefinidos

    // Caso de prueba 1
    vector<int> a1 = {999999,1000000};
    cout << "Test Case 1: ";
    cout << "Array = {1, 2, 3, 4, 5}" << endl;
    cout << "Element closest to all other values: " << solution(a1) << endl;

    // Caso de prueba 2
    vector<int> a2 = {10, 20, 30, 40, 50};
    cout << "Test Case 2: ";
    cout << "Array = {10, 20, 30, 40, 50}" << endl;
    cout << "Element closest to all other values: " << solution(a2) << endl;

    // Caso de prueba 3
    vector<int> a3 = {-5, 0, 5, 10, 15};
    cout << "Test Case 3: ";
    cout << "Array = {-5, 0, 5, 10, 15}" << endl;
    cout << "Element closest to all other values: " << solution(a3) << endl;

    // Caso de prueba 4
    vector<int> a4 = {2, 4, 6, 8, 10};
    cout << "Test Case 4: ";
    cout << "Array = {2, 4, 6, 8, 10}" << endl;
    cout << "Element closest to all other values: " << solution(a4) << endl;

    // Caso de prueba 5
    vector<int> a5 = {1, 2, 3, 4, 5, 6, 7};
    cout << "Test Case 5: ";
    cout << "Array = {1, 2, 3, 4, 5, 6, 7}" << endl;
    cout << "Element closest to all other values: " << solution(a5) << endl;

    return 0;
}
*/