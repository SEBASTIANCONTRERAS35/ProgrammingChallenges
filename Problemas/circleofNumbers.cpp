#include <iostream>
using namespace std;
/*
int solution(int n, int firstNumber){
    return (firstNumber + n / 2) % n;
}

int main() {
    int n, firstNumber;

    // Solicitar al usuario que ingrese el número de enteros y el primer número
    cout << "Enter the number of integers (n): ";
    cin >> n;

    cout << "Enter the first number (firstNumber): ";
    cin >> firstNumber;

    // Llamar a la función solution y mostrar el resultado
    int result = solution(n, firstNumber);
    cout << "The number in the radially opposite position is: " << result << endl;

    return 0;
}
*/